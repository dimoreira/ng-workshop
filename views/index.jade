extends ./layout.jade

block content
	.container-fluid(ng-controller="MainCtrl")
		.col-md-offset-3.col-md-6
			h2 {{ title }}

			ul.nav.nav-sidebar
				li(ng-repeat="item in menu")
					a(href="#") {{ item }}
